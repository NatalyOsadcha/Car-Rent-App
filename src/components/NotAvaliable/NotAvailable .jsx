import {NotAvailableWrapper, Message} from './NotAvailable .styled'
export default function NotAvailable ({ text }) {
    return (
        <NotAvailableWrapper>
          <Message>{text}</Message>
        </NotAvailableWrapper>
      )}
